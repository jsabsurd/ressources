sudo pacman -Syu
sudo pacma -S firefox git obsidian kleopatra keepassxc dosfstools curl wget feh zsh ttf-inconsolata xorg-server xorg-xinit libxft libxinerama vim

clone dwm/st/dmenu
make x3

cd ~
sudo echo "exec dwm" >> .xinitrc

vim curl command
zsh command
zsh
clone configs
	einbetten (.vim ohne sudo kopieren, sonst PlugInstall keine Berechtigung!)
source .zshrc

reboot

---------------

usb stick:
	Datenbank
	obsidian
vim PlugInstall
git config files adden
