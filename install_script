sudo pacman -Syu
sudo pacman -S firefox git obsidian kleopatra keepassxc dosfstools curl wget feh zsh ttf-inconsolata xorg-server xorg-xinit libxft libxinerama vim

clone dwm/st/dmenu
make x3

cd ~
sudo echo "exec dwm" >> .xinitrc

vim curl command
zsh command
zsh
clone configs
	einbetten (.vim ohne sudo kopieren, sonst PlugInstall keine Berechtigung!)
source .zshrc

reboot

[yay installieren]
---------------

vim PlugInstall
git config files adden
usb stick:
	Datenbank
	obsidian
